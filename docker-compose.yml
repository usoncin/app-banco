# Use root/example as user/password credentials

services:
  db:
    image: mysql:8
    restart: always
    ports:
      - 33066:3306
    environment:
      LANG: C.UTF-8
      MYSQL_ROOT_PASSWORD: example
      MYSQL_DATABASE: ${DB_NAME}
    volumes:
      - ./bbdd/mysql:/var/lib/mysql
      - ./bbdd/initData:/docker-entrypoint-initdb.d

  phpmyadmin:
    image: phpmyadmin
    restart: always
    ports:
      - 5555:80
    # environment:
    #   - PMA_HOST=db
